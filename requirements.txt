# Core runtime dependencies
networkx>=3.1
numpy>=1.24

# Claude backend for generation and LLM-based entity extraction.
# Without an ANTHROPIC_API_KEY the pipeline falls back to the built-in
# offline extractive answerer, so this is optional but installed by default.
anthropic>=0.69.0

# Reads .env for configuration (optional, loaded lazily).
python-dotenv>=1.0.1
